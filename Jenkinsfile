@Library('MicroserviceBuilder') _
microserviceBuilderPipeline {
  image = 'conferenceschedule'
  // mvnCommands = 'clean package -P security'
}

// Uncomment mvnCommands to use the secured version of this microservice